types:
  tor1_double: &tor1_double
    type: array
    subtype: double
    size: &tor1_size [ '$tor1_extents[0]' ]

  tor2_double: &tor2_double
    type: array
    subtype: double
    size: &tor2_size [ '$tor2_extents[0]' ]

  tor3_double: &tor3_double
    type: array
    subtype: double
    size: &tor3_size [ '$tor3_extents[0]' ]

  vpar_double: &vpar_double
    type: array
    subtype: double
    size: &vpar_size [ '$vpar_extents[0]' ]

  mu_double: &mu_double
    type: array
    subtype: double
    size: &mu_size [ '$mu_extents[0]' ]

metadata:
  gysela_io_filename_size: size_t
  gysela_io_filename: {type: array, subtype: char, size: "$gysela_io_filename_size"}

  #-- Parallel data
  local_fdistribu_starts: { type: array, subtype: size_t, size: 6 }
  local_fdistribu_extents: { type: array, subtype: size_t, size: 6 }

  #-- Species info
  species:
    type: array
    subtype: int
    size: [ '$species_extents[0]' ]
  masses:
    type: array
    subtype: double
    size: [ '$masses_extents[0]' ]
  charges:
    type: array
    subtype: double
    size: [ '$charges_extents[0]' ]
  species_extents: { type: array, subtype: size_t, size: 1 }
  masses_extents: { type: array, subtype: size_t, size: 1 }
  charges_extents: { type: array, subtype: size_t, size: 1 }

  #-- Mesh: breakpoints and grid
  breakpoints_tor1_extents: { type: array, subtype: size_t, size: 1 }
  breakpoints_tor2_extents: { type: array, subtype: size_t, size: 1 }
  breakpoints_tor3_extents: { type: array, subtype: size_t, size: 1 }
  breakpoints_vpar_extents: { type: array, subtype: size_t, size: 1 }
  breakpoints_mu_extents: { type: array, subtype: size_t, size: 1 }
  tor1_extents: { type: array, subtype: size_t, size: 1 }
  tor2_extents: { type: array, subtype: size_t, size: 1 }
  tor3_extents: { type: array, subtype: size_t, size: 1 }
  vpar_extents: { type: array, subtype: size_t, size: 1 }
  mu_extents: { type: array, subtype: size_t, size: 1 }
  breakpoints_tor1:
    type: array
    subtype: double
    size: [ '$breakpoints_tor1_extents[0]' ]
  breakpoints_tor2:
    type: array
    subtype: double
    size: [ '$breakpoints_tor2_extents[0]' ]
  breakpoints_tor3:
    type: array
    subtype: double
    size: [ '$breakpoints_tor3_extents[0]' ]
  breakpoints_vpar:
    type: array
    subtype: double
    size: [ '$breakpoints_vpar_extents[0]' ]
  breakpoints_mu:
    type: array
    subtype: double
    size: [ '$breakpoints_mu_extents[0]' ]
  tor1: tor1_double
  tor2: tor2_double
  tor3: tor3_double
  vpar: vpar_double
  mu: mu_double

  # -- Timing statistics
  timing_names_count: size_t
  timing_names_strlen: size_t
  timing_names: { type: array, subtype: char, size: [ '$timing_names_count', '$timing_names_strlen' ] }
  timing_values_size: size_t
  timing_values: { type: array, subtype: double, size: [ '$timing_values_size' ] }

  #-- Fluid moments extents
  density_extents: { type: array, subtype: size_t, size: 4 }
  mean_velocity_extents: { type: array, subtype: size_t, size: 4 }
  temperature_extents: { type: array, subtype: size_t, size: 4 }

  iteration: int
data:
  #-- Distribution function
  fdistribu_sptor3Dv2D:
    type: array
    subtype: double
    size: [ '$local_fdistribu_extents[0]', '$local_fdistribu_extents[1]', '$local_fdistribu_extents[2]', '$local_fdistribu_extents[3]', '$local_fdistribu_extents[4]', '$local_fdistribu_extents[5]' ]

  #-- Fluid moments (4D: species × tor1 × tor2 × tor3)
  density:
    type: array
    subtype: double
    size: [ '$density_extents[0]', '$density_extents[1]', '$density_extents[2]', '$density_extents[3]' ]
  mean_velocity:
    type: array
    subtype: double
    size: [ '$mean_velocity_extents[0]', '$mean_velocity_extents[1]', '$mean_velocity_extents[2]', '$mean_velocity_extents[3]' ]
  temperature:
    type: array
    subtype: double
    size: [ '$temperature_extents[0]', '$temperature_extents[1]', '$temperature_extents[2]', '$temperature_extents[3]' ]
  spec0_temp:
    type: array
    subtype: double
    size: [ '$temperature_extents[1]', '$temperature_extents[2]', '$temperature_extents[3]' ]
  spec1_temp:
    type: array
    subtype: double
    size: [ '$temperature_extents[1]', '$temperature_extents[2]', '$temperature_extents[3]' ]
# pdi:
#   metadata:
#     iter:   int                    # current iteration id
#     dsize:  { size: 2, type: array, subtype: int } # local data size including ghosts/boundary
#     psize:  { size: 2, type: array, subtype: int } # number of processes in each dimension
#     pcoord: { size: 2, type: array, subtype: int } # coordinate of the process
#     mpi_comm: MPI_Comm
#     conf_yaml: uintptr_t
#   data:
#     main_field: { size: [ '$dsize[0]', '$dsize[1]' ], type: array, subtype: double }
  
plugins:
  mpi:
    
  decl_hdf5:

    #-- Write timing (as table: names and values arrays)
    - file: 'cpu_time.h5'
      on_event: [write_timing]
      collision_policy: replace_and_warn
      write:
        timing_names: ~
        timing_values: ~
        
    #-- Write distribution function and coordinates
    - file: fdistribu_5D_output.h5
      on_event: write_fdistribu
      collision_policy: replace_and_warn
      communicator: $MPI_COMM_WORLD
      datasets:
        fdistribu_sptor3Dv2D:
          type: array
          subtype: double
          size: [ '$species_extents[0]', '$tor1_extents[0]', '$tor2_extents[0]', '$tor3_extents[0]', '$vpar_extents[0]', '$mu_extents[0]' ]
      write:
        tor1: ~
        tor2: ~
        tor3: ~
        vpar: ~
        mu: ~
        fdistribu_sptor3Dv2D:
            dataset_selection:
              size: [ '$local_fdistribu_extents[0]', '$local_fdistribu_extents[1]', '$local_fdistribu_extents[2]', '$local_fdistribu_extents[3]', '$local_fdistribu_extents[4]', '$local_fdistribu_extents[5]' ]
              start: [ '$local_fdistribu_starts[0]', '$local_fdistribu_starts[1]', '$local_fdistribu_starts[2]', '$local_fdistribu_starts[3]', '$local_fdistribu_starts[4]', '$local_fdistribu_starts[5]' ]
  
    #-- Write fluid moments
    - file: 'fluid_moments.h5'
      on_event: [write_fluid_moments]
      collision_policy: replace_and_warn
      write:
        density: ~
        mean_velocity: ~
        temperature: ~

    - file: 'spec0_temp.h5'
      on_event: [write_reduced_temperature]
      collision_policy: replace_and_warn
      write:
        spec0_temp: ~
        # spec1_temp: ~
    
    
  trace: ~
  pycall:
    on_event:
      Init:
        with: {}
        exec: |
          import numpy as np
          import xarray as xr
          from fluid_moments import FluidMoments
      FluidMoments:
        with: {fdistribu_raw: $fdistribu_sptor3Dv2D, vpar_vals: $vpar, mu_vals: $mu, iter: $iteration}
        exec: |
          fdistribu = xr.DataArray(
              fdistribu_raw,
              dims=('species', 'tor1', 'tor2', 'tor3', 'vpar', 'mu')
          )
          vpar = xr.DataArray(vpar_vals, dims=('vpar'))
          mu = xr.DataArray(mu_vals, dims=('mu'))
          fm = FluidMoments(vpar, mu)

          density = fm.compute_density(fdistribu).compute()
          velocity = fm.compute_velocity(fdistribu, density).compute()
          temperature = fm.compute_temperature(fdistribu, density, velocity).compute()

          ds_out = xr.Dataset({
              'density': density[0,:,:,0],
              'velocity': velocity,
              'temperature': temperature,
          })
          file_name = 'fluid_moments_iter'+str(iter)+'.nc'
          ds_out.to_netcdf(file_name)

    # damaris:
    #   architecture: 
    #     sim_name: gysela-miniapp  
    #     domains: 1
    #     dedicated:
    #       core: 1
    #       node: 0 
    #   client_comm_get: mpi_comm # Needed to return the Damaris client comm
    #   datasets:
    #     - dataset: 
    #         name: main_field
    #         layout: main_field_layout
    #         storage: hdf5_example
    #   layouts:
    #     - layout: 
    #         name: main_field_layout
    #         type: double
    #         global: ['$psize[0]*($dsize[0]-2)', '$psize[1]*($dsize[1]-2)']
    #         dimensions: [ '$dsize[0]', '$dsize[1]' ] # process dim, with ghosts/boundaries
    #         ghosts: '1:1,1:1'
    #         depends_on: [dsize, psize] # This will help ensure an update of the layout attributes value to Damaris lib once the metadata are exposed (using Damaris Parameters in the background)!
    #   storages:
    #     - storage:
    #         name: hdf5_example
    #         type: HDF5
    #         file_mode: FilePerCore       # or FilePerCore
    #         files_path: ./HDF5_files_without_is_client/   # Where to save files
          
    #   write: 
    #     main_field: # the name of the data to write, if dataset not specified afterward! ~meme notion de dataset hdf5
    #       dataset: main_field 
    #       when: '$iter<10'   # do only write the first 10 iterations (0...9), Default at every iteration.
    #       position: ['($dsize[0]-2)*$pcoord[0]', '($dsize[1]-2)*$pcoord[1]'] # ~start de dataset_selection par iteration

    #   log:
    #     file_name: example_without_is_client
    #     rotation_size: 5
    #     log_level: info
    #     flush: true